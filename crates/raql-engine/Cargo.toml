[package]
name = "raql-engine"
version = "0.0.0"
edition = "2024"

[lib]
doctest = false

[dependencies]
raql-compiler = { path = "../raql-compiler" }
raql-plan = { path = "../raql-plan" }
raql-syntax = { path = "../raql-syntax" }

indexmap.workspace = true
thiserror.workspace = true
